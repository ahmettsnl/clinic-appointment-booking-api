from pymongo import MongoClient
from dotenv import load_dotenv
import os

load_dotenv()

MONGO_URL = os.getenv("MONGO_URL")

client = MongoClient(MONGO_URL)

mongodb = client["clinic_db"]

patient_records_collection = mongodb["patient_records"]